name: Input Demo

on:

  workflow_dispatch:

    inputs:

      environment:

        description: "Environment"

        required: true

        default: DEV

jobs:

  demo:

    runs-on: ubuntu-latest

    steps:

      - run: echo "Deploying to ${{ github.event.inputs.environment }}"