name: Hello Workflow

on:

  push:

    branches:

      - master

jobs:

  hello:

    runs-on: ubuntu-latest

    steps:

      - name: Print message

        run: echo "Hello There"

      - name: Print date

        run: date