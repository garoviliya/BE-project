name: Secret Demo

on:

  workflow_dispatch:

jobs:

  demo:

    runs-on: ubuntu-latest

    steps:

      - run: echo "Using secret"

        env:

          API_KEY: ${{ secrets.ILIYA_SECRET }}

      - run: echo "Secret length OK"